[package]
name = "ndx"
version = "0.1.0"
edition = "2021"
description = "Rust bindings for ndx"
repository = "https://github.com/quirinpa/ndx"

[lib]
crate-type = ["cdylib", "rlib"]

[dependencies]
libc = { version = "=0.2.139", default-features = false }

[profile.release]
lto = true
