[package]
name = "mod-rust-caller"
version = "0.1.0"
edition = "2021"

[lib]
name = "mod_rust_caller"
crate-type = ["cdylib"]

[dependencies]
ndx = { path = "../../../ndx" }

[profile.dev]
panic = "abort"

[profile.release]
panic = "abort"
